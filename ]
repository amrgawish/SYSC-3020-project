<h1>Listing Events</h1>
<%= link_to 'New Topic', new_event_path %>
<table>
  <tr>
    <th> Author </th>
    <th> Description</th>
    <th> Start Time </th>
    <th> End Time </th>
    <th> Class  </th>
    <th colspan = "5"></th>
  </tr>

  <% @event.each do |event| %>
    <tr>
      <td><%= event.name %></td>
      <td><%= event.description %></td>
      <td><%= event.start_time %></td>
      <td><%= event.end_time %></td>
      <td><%= event.class %></td>

      <td><%= link_to 'Edit', edit_event_path(event) %></td>
      <td><%= link_to 'Delete', event_path(event), method: :delete, data:{ confirm: 'Are you sure you want to delete this topic?' } %></td>
    </tr>
  <%end%> 
</table>
